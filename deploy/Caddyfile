# Template only; no cloud services have been provisioned.
# The API enforces owner basic authentication in APP_MODE=live.
{$SITE_DOMAIN} {
    encode zstd gzip
    reverse_proxy 127.0.0.1:8000
    header {
        X-Content-Type-Options nosniff
        Referrer-Policy same-origin
        X-Frame-Options DENY
    }
}
